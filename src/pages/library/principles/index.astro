---
import Layout from "@layouts/Layout.astro";
import Container from "@components/Container.astro";
import ArrowCard from "@components/ArrowCard.astro";
import { LIBRARY } from "@consts";
import {
  getDocumentsByCollection,
  sortDocumentsByDate,
} from "@lib/documents";

const principles = sortDocumentsByDate(
  getDocumentsByCollection("library/principles"),
);
---

<Layout title="Library · Principles" description={LIBRARY.DESCRIPTION}>
  <Container>
    <aside data-pagefind-ignore>
      <div class="space-y-8">
        <div class="space-y-2">
          <h1 class="text-3xl font-semibold text-black dark:text-white">
            Principles
          </h1>
          <p class="max-w-2xl text-black/70 dark:text-white/70">
            Core ideas that guide my work and life.
          </p>
        </div>
        {principles.length === 0 ? (
          <p class="text-sm text-black/60 dark:text-white/60">
            No principles yet.
          </p>
        ) : (
          <ul class="not-prose flex flex-col gap-4">
            {principles.map((doc) => (
              <li>
                <ArrowCard entry={doc} />
              </li>
            ))}
          </ul>
        )}
      </div>
    </aside>
  </Container>
</Layout>
